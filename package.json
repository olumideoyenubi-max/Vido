{
  "name": "vido",
  "version": "0.1.0",
  "description": "AI video generator: turn text prompts (and optional images) into video clips via pluggable providers.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
